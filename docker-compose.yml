services:
  listly:
    build:
      context: .
    image: listly:latest
    container_name: listly
    restart: unless-stopped
    ports:
      - "${HOST_PORT:-3001}:3000"
    environment:
      PORT: "3000"
      DATA_DIR: /data
    volumes:
      - listly-data:/data

volumes:
  listly-data:

services:
  listly:
    build:
      context: .
    image: listly:latest
    container_name: listly
    restart: unless-stopped
    ports:
      - "3000:3000"
    environment:
      PORT: "3000"
      DATA_DIR: /data
    volumes:
      - listly-data:/data

volumes:
  listly-data:
